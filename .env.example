# Flask API Configuration
API_HOST=localhost
API_PORT=5000
API_DEBUG=True

# PHP Web Configuration
WEB_HOST=localhost
WEB_PORT=8000

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=signaura_db

# Model Paths (relative to api/ directory)
MODEL_PATH=../ml-models/model.keras
ENCODER_PATH=../ml-models/label_encoder_382.pkl
LABELS_PATH=../ml-models/labels_new.csv

# CORS Settings (for frontend communication)
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
